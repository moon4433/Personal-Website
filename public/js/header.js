

function setup() {
  let cnv = createCanvas(displayWidth, displayHeight, WEBGL);
  cnv.parent('header-canvas');
  // Create objects
  
}

function draw() {
  background(18, 18, 18);
  translate(100,100,-100);
  box();
}
